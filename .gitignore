# ESP-IDF build products of the hardware test bed (hardware-tests/, the former firmware/)
hardware-tests/build/
hardware-tests/sdkconfig
hardware-tests/sdkconfig.old
hardware-tests/sdkconfig.secrets
hardware-tests/managed_components/
hardware-tests/dependencies.lock.old
hardware-tests/tools/gifcheck/build/

# The product firmware (firmware/): same ESP-IDF layout assumed until the project
# takes shape; credentials must never be committed from either folder.
firmware/build/
firmware/sdkconfig
firmware/sdkconfig.old
firmware/sdkconfig.secrets
firmware/managed_components/
firmware/dependencies.lock.old

# Upstream clones kept locally for reference (see firmware/README.md)
firmware/reference/
firmware/tests/host/build*/

# Editors and OS
.vscode/
.idea/
.cache/
*.swp
Thumbs.db
.DS_Store
__pycache__/

# Private outreach log (posts, metrics, feedback); kept local on purpose
docs/outreach.md
