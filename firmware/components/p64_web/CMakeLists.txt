# p64_web: the HTTP API (/api/v1), the WebSocket status push, the live preview, the card
# file manager, and the embedded web UI. docs/architecture.md section 8, spec 11-12.
idf_component_register(
    SRCS
        "src/api.cpp"
        "src/files.cpp"
        "src/content.cpp"
        "src/makapix_routes.cpp"
        "src/ws.cpp"
        "src/ui.cpp"
        "src/auth.cpp"
    INCLUDE_DIRS
        "include"
    EMBED_TXTFILES
        "ui/index.html"
    REQUIRES
        p64_gfx
        p64_display
        p64_net
        p64_system
        json
    PRIV_REQUIRES
        p64_storage
        p64_content
        p64_makapix
        p64_widgets
        p64_stream
        p64_decode
        p64_playback
        esp_timer
        esp_app_format
        app_update
        heap
        nvs_flash
)
