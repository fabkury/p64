## IDF Component Manager manifest for the p64 application.
## esphome/esp-hub75, bitbank2/AnimatedGIF, libpng (APNG patch) and libwebp are vendored
## under components/. Managed components download into firmware/managed_components/ on
## the first build; dependencies.lock pins the exact versions and is committed.
dependencies:
  idf: ">=5.5"
  espressif/mdns: "^1.8"
