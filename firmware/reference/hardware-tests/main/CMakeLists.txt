# No GIF is embedded in the binary: artwork comes from the microSD card at boot and
# from Makapix Club afterwards (the test corpus of tools/gifcheck is
# firmware/tests/host/corpus/gifs, the former assets/gifs of this folder).
idf_component_register(
    SRCS
        "main.cpp"
        "display.cpp"
        "button.cpp"
        "gif_player.cpp"
        "sdcard.cpp"
        "net/wifi.cpp"
        "net/clock.cpp"
        "net/makapix.cpp"
        "net/speedtest.cpp"
        "net/web.cpp"
        "scenes/gif_show.cpp"
    INCLUDE_DIRS
        "."
    PRIV_REQUIRES
        esp-hub75
        animatedgif
        driver
        esp_timer
        nvs_flash
        esp_wifi
        esp_netif
        esp_event
        lwip
        esp_http_client
        esp-tls
        mbedtls
        json
        esp_app_format
        esp_http_server
        espressif__mdns
        fatfs
        sdmmc
        esp_driver_sdmmc
        vfs
)
