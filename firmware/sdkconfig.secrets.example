# Copy to sdkconfig.secrets (git-ignored) to seed development Wi-Fi credentials into NVS
# on a device whose NVS holds none. Release builds must not carry these values: the
# product stores credentials from the setup portal only.
CONFIG_P64_DEV_WIFI_SSID="my-network"
CONFIG_P64_DEV_WIFI_PASSWORD="my-password"
